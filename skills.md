# Popular Food Shop — 電商網站開發技能規格書

> **專案名稱：** Popular Food Shop  
> **參考風格：** [IFCHIC](https://www.ifchic.com/tw/) — 精品電商風格  
> **目標：** 開發一個高質感、安全、高效能的食品電商平台  
> **日期：** 2026-02-21

---

## 目錄

1. [專案總覽](#1-專案總覽)
2. [技術棧](#2-技術棧)
3. [系統架構](#3-系統架構)
4. [前端功能規劃](#4-前端功能規劃)
5. [後端 API 設計](#5-後端-api-設計)
6. [資料庫設計](#6-資料庫設計)
7. [管理者後台](#7-管理者後台)
8. [網路安全與個資保護](#8-網路安全與個資保護)
9. [效能規劃（5000 人同時上線）](#9-效能規劃5000-人同時上線)
10. [部署策略（Vercel）](#10-部署策略vercel)
11. [Chrome MCP 驗證計畫](#11-chrome-mcp-驗證計畫)
12. [開發時程與里程碑](#12-開發時程與里程碑)
13. [目錄結構](#13-目錄結構)

---

## 1. 專案總覽

### 1.1 專案定位

Popular Food Shop 是一個以精品食品為主題的電商平台，風格參考 IFCHIC 的高端精品電商設計，具備：

- **精緻視覺體驗**：Hero Banner 輪播、分類卡片式佈局、商品瀑布流展示
- **完整購物流程**：瀏覽 → 加入購物車 → 結帳 → 付款 → 訂單追蹤
- **會員系統**：註冊/登入、個人資料、收藏清單、訂單紀錄、回饋金/點數
- **管理者後台**：商品管理、訂單管理、會員管理、促銷管理、數據儀表板
- **多語系支援**：繁體中文（預設）、英文

### 1.2 核心業務功能

| 功能模組 | 說明                                         |
| -------- | -------------------------------------------- |
| 商品展示 | 分類瀏覽、品牌篩選、搜尋、折扣標籤、新品標記 |
| 購物車   | 即時數量調整、優惠碼、運費計算               |
| 結帳付款 | 信用卡（綠界/藍新）、LINE Pay、貨到付款      |
| 會員中心 | 個人資料、地址簿、信用卡管理、訂單/退貨紀錄  |
| 回饋機制 | 購物回饋金、註冊獎勵、推薦獎勵               |
| 促銷活動 | 限時特賣、折扣碼、滿額免運、組合優惠         |
| 內容管理 | 首頁 Banner、分類頁 Banner、品牌故事         |

---

## 2. 技術棧

### 2.1 前端

| 技術                     | 版本  | 用途                        |
| ------------------------ | ----- | --------------------------- |
| **Vue 3**                | ^3.5  | 前端框架（Composition API） |
| **Vite**                 | ^6.x  | 建置工具、開發伺服器、HMR   |
| **Pinia**                | ^2.x  | 狀態管理（取代 Vuex）       |
| **Vue Router**           | ^4.x  | SPA 路由管理                |
| **JavaScript (ES2022+)** | —     | 開發語言                    |
| **Tailwind CSS**         | ^4.x  | Utility-first CSS 框架      |
| **Axios**                | ^1.x  | HTTP 請求庫                 |
| **VueUse**               | ^12.x | Composition API 工具集      |
| **Swiper**               | ^11.x | 輪播/滑動元件               |
| **vue-i18n**             | ^10.x | 多語系                      |
| **vee-validate + yup**   | —     | 表單驗證                    |
| **vue-lazyload**         | —     | 圖片懶載入                  |
| **nprogress**            | —     | 頁面載入進度條              |

### 2.2 後端

| 技術                 | 版本   | 用途                                 |
| -------------------- | ------ | ------------------------------------ |
| **Python**           | ^3.12  | 後端語言                             |
| **FastAPI**          | ^0.115 | 高效能非同步 Web 框架                |
| **Uvicorn**          | ^0.34  | ASGI 伺服器                          |
| **SQLAlchemy**       | ^2.x   | ORM / 資料庫操作                     |
| **Alembic**          | ^1.x   | 資料庫遷移工具                       |
| **Pydantic**         | ^2.x   | 資料驗證與序列化                     |
| **python-jose**      | —      | JWT Token 處理                       |
| **passlib[bcrypt]**  | —      | 密碼雜湊                             |
| **celery**           | ^5.x   | 非同步任務佇列（寄信、報表）         |
| **Redis**            | ^7.x   | 快取、Session、Rate Limiting         |
| **Pillow**           | —      | 圖片處理                             |
| **boto3**            | —      | AWS S3 / Cloudflare R2 檔案上傳      |
| **python-multipart** | —      | 檔案上傳處理                         |
| **httpx**            | —      | 非同步 HTTP 客戶端（第三方支付串接） |

### 2.3 資料庫

| 技術           | 版本  | 用途                         |
| -------------- | ----- | ---------------------------- |
| **PostgreSQL** | ^16.x | 主要關聯式資料庫             |
| **Redis**      | ^7.x  | 快取層、Session 儲存、排行榜 |

### 2.4 開發與部署工具

| 技術                          | 用途                           |
| ----------------------------- | ------------------------------ |
| **Docker / Docker Compose**   | 容器化開發與部署環境           |
| **GitHub Actions**            | CI/CD 自動化                   |
| **Vercel**                    | 前端部署                       |
| **Railway / Render / Fly.io** | 後端 + PostgreSQL + Redis 部署 |
| **Cloudflare R2**             | 靜態資源 / 圖片 CDN            |
| **ESLint + Prettier**         | 前端程式碼品質                 |
| **Ruff**                      | Python Linter / Formatter      |
| **pytest**                    | 後端測試框架                   |
| **Vitest**                    | 前端單元測試                   |
| **Playwright**                | E2E 測試                       |

---

## 3. 系統架構

### 3.1 整體架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                        使用者瀏覽器                           │
│                    (Vue 3 SPA + Tailwind)                    │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                    Vercel（前端 CDN 部署）                      │
│           - SSG / SPA                                        │
│           - Edge Functions (API Proxy)                        │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS (API Calls)
                           ▼
┌──────────────────────────────────────────────────────────────┐
│               Nginx / Traefik (反向代理 + SSL)                 │
│           - Rate Limiting                                    │
│           - CORS Headers                                     │
│           - Gzip / Brotli 壓縮                                │
└──────────┬───────────────────────────────────┬──────────────┘
           │                                   │
           ▼                                   ▼
┌─────────────────────┐          ┌──────────────────────────┐
│   FastAPI Backend    │          │        Celery Worker     │
│   (Uvicorn x4)      │◄────────►│   (非同步任務)             │
│   - REST API        │          │   - 寄送 Email            │
│   - JWT Auth        │          │   - 報表生成              │
│   - Business Logic  │          │   - 圖片壓縮處理           │
└────┬──────────┬─────┘          └────────────┬────────────┘
     │          │                              │
     ▼          ▼                              ▼
┌──────────┐ ┌──────────┐              ┌──────────────┐
│PostgreSQL│ │  Redis   │              │ Cloudflare   │
│  (主DB)  │ │  (快取)  │              │ R2 / S3      │
│          │ │ Session  │              │ (檔案儲存)    │
└──────────┘ └──────────┘              └──────────────┘
```

### 3.2 前後端分離原則

- **前端（Vercel）**：純粹負責 UI 渲染與使用者互動
- **後端（FastAPI）**：提供 RESTful API，處理業務邏輯
- **CORS 嚴格設定**：僅允許前端網域呼叫 API
- **API 版本控制**：所有路由以 `/api/v1/` 為前綴

---

## 4. 前端功能規劃

### 4.1 頁面結構（參照 IFCHIC 風格）

#### 🏠 首頁 (`/`)

| 區塊            | 說明                                                                 |
| --------------- | -------------------------------------------------------------------- |
| 頂部公告列      | 免運門檻提示、促銷訊息（可關閉）                                     |
| 導覽列 (Navbar) | Logo、搜尋、會員、願望清單、購物車圖示                               |
| Mega Menu       | 滑鼠 hover 展開多層分類選單                                          |
| Hero Banner     | 全幅輪播圖（Swiper），附 CTA 按鈕「立即選購」                        |
| 分類選品區      | 四宮格卡片展示（如：人氣零食 2折起、精選茶葉、進口巧克力、養生穀物） |
| 熱搜標籤列      | 水平滾動標籤（如：日本零食、有機食品、手工餅乾…）                    |
| 主題大圖區      | 左右對比式雙欄大圖 + 標題 + CTA（仿 IFCHIC 針織/外套區）             |
| 精選商品輪播    | 水平滾動商品卡片（商品圖、品牌名、品名、原價、折扣價、折扣標籤）     |
| 品牌故事區      | 四宮格品牌圖 + 折扣資訊                                              |
| 新品上架區      | 雙欄大圖（男/女 → 改為 鹹食/甜食 或 東方/西方）                      |
| 信任指標區      | 三欄 icon + 文字（100% 正品保證、專業品質控管、全台冷鏈配送）        |
| 回饋金推廣區    | Banner + 說明 + CTA                                                  |
| Footer          | 關於我們、服務條款、隱私政策、運送資訊、退換貨、社群連結             |

#### 📦 商品列表頁 (`/category/:slug`)

- 左側：篩選面板（品牌、價格區間、分類、評價、產地）
- 右側：商品網格（2/3/4欄切換）
- 排序：最新、熱銷、價格高→低、價格低→高、折扣
- 無限滾動載入 或 分頁
- URL Query 同步篩選狀態

#### 🔍 搜尋頁 (`/search?q=`)

- 即時搜尋建議（Debounce 300ms）
- 搜尋結果 + 篩選
- 無結果時推薦商品

#### 🛒 商品詳情頁 (`/product/:id`)

- 大圖輪播（支援縮放）
- 品牌名稱（可連結至品牌頁）
- 商品名稱、價格（原價 / 折扣價）
- 規格選擇（重量、口味、組合）
- 數量選擇器
- 加入購物車 / 加入願望清單
- 商品描述、營養標示、過敏原資訊
- 相關商品推薦
- 顧客評價

#### 🛍 購物車頁 (`/cart`)

- 商品清單（圖片、名稱、單價、數量調整、小計、刪除）
- 優惠碼輸入
- 回饋金折抵
- 運費計算
- 訂單摘要
- 前往結帳按鈕

#### 💳 結帳頁 (`/checkout`)

- Step 1：收件資訊（姓名、電話、地址、備註）
- Step 2：配送方式（宅配、超商取貨）
- Step 3：付款方式（信用卡、LINE Pay、貨到付款）
- Step 4：確認訂單 → 送出
- 地址簿快速選擇

#### 👤 會員中心 (`/account/*`)

| 子頁         | 路由                 | 功能                        |
| ------------ | -------------------- | --------------------------- |
| 個人資料     | `/account/profile`   | 修改姓名、Email、密碼、頭像 |
| 我的地址     | `/account/addresses` | 新增/編輯/刪除地址          |
| 我的信用卡   | `/account/cards`     | 儲存卡片管理                |
| 訂單紀錄     | `/account/orders`    | 訂單列表、詳情、追蹤物流    |
| 退貨紀錄     | `/account/returns`   | 退貨申請、進度追蹤          |
| 我的願望清單 | `/account/wishlist`  | 收藏商品管理                |
| 回饋金       | `/account/points`    | 點數明細、到期提醒          |
| 購物金       | `/account/credits`   | 購物金餘額與紀錄            |

#### 📝 其他頁面

- `/brands` — 品牌列表（A-Z 索引）
- `/brand/:slug` — 品牌專頁
- `/about` — 關於我們
- `/contact` — 聯絡我們
- `/faq` — 常見問題
- `/terms` — 服務條款
- `/privacy` — 隱私政策
- `/shipping` — 運送資訊
- `/returns` — 退換貨說明
- `/login` — 登入
- `/register` — 註冊
- `/forgot-password` — 忘記密碼
- `/reset-password` — 重設密碼

### 4.2 前端元件架構

```
src/
├── components/
│   ├── layout/
│   │   ├── AppHeader.vue          # 頂部導覽列
│   │   ├── AppFooter.vue          # 底部頁尾
│   │   ├── AnnouncementBar.vue    # 公告列
│   │   ├── MegaMenu.vue           # 多層選單
│   │   └── MobileMenu.vue         # 行動版選單
│   ├── home/
│   │   ├── HeroBanner.vue         # 首頁輪播大圖
│   │   ├── CategoryGrid.vue       # 分類四宮格
│   │   ├── TrendingTags.vue       # 熱搜標籤
│   │   ├── FeaturedSection.vue    # 主題大圖區
│   │   ├── ProductCarousel.vue    # 商品輪播
│   │   ├── BrandShowcase.vue      # 品牌展示區
│   │   ├── TrustBadges.vue        # 信任指標
│   │   └── RewardsPromo.vue       # 回饋金推廣
│   ├── product/
│   │   ├── ProductCard.vue        # 商品卡片
│   │   ├── ProductGallery.vue     # 商品圖片輪播
│   │   ├── ProductInfo.vue        # 商品資訊
│   │   ├── ProductReviews.vue     # 評價區
│   │   ├── QuantitySelector.vue   # 數量選擇
│   │   └── RelatedProducts.vue    # 相關推薦
│   ├── cart/
│   │   ├── CartItem.vue           # 購物車項目
│   │   ├── CartSummary.vue        # 訂單摘要
│   │   └── CouponInput.vue        # 優惠碼輸入
│   ├── checkout/
│   │   ├── CheckoutSteps.vue      # 結帳步驟指示器
│   │   ├── ShippingForm.vue       # 收件資訊表單
│   │   ├── PaymentForm.vue        # 付款方式選擇
│   │   └── OrderConfirm.vue       # 訂單確認
│   ├── account/
│   │   ├── ProfileForm.vue        # 個人資料表單
│   │   ├── AddressBook.vue        # 地址簿
│   │   ├── OrderList.vue          # 訂單列表
│   │   └── PointsHistory.vue      # 回饋金紀錄
│   └── ui/
│       ├── BaseButton.vue         # 通用按鈕
│       ├── BaseInput.vue          # 通用輸入框
│       ├── BaseModal.vue          # 通用彈窗
│       ├── BaseBadge.vue          # 標籤/徽章
│       ├── BaseToast.vue          # 通知提示
│       ├── BasePagination.vue     # 分頁
│       ├── BaseSkeleton.vue       # 載入骨架
│       └── BaseDropdown.vue       # 下拉選單
```

### 4.3 Pinia Store 規劃

| Store              | 職責                                  |
| ------------------ | ------------------------------------- |
| `useAuthStore`     | 登入狀態、Token、使用者資訊           |
| `useCartStore`     | 購物車項目、數量、小計                |
| `useProductStore`  | 商品列表、篩選、搜尋結果快取          |
| `useWishlistStore` | 願望清單                              |
| `useCheckoutStore` | 結帳流程狀態                          |
| `useUIStore`       | 全域 UI 狀態（Modal、Toast、Loading） |
| `useI18nStore`     | 語系切換                              |

### 4.4 響應式設計斷點（Tailwind）

| 斷點  | 寬度    | 裝置         |
| ----- | ------- | ------------ |
| `sm`  | ≥640px  | 手機（橫向） |
| `md`  | ≥768px  | 平板         |
| `lg`  | ≥1024px | 小型筆電     |
| `xl`  | ≥1280px | 桌機         |
| `2xl` | ≥1536px | 大螢幕       |

---

## 5. 後端 API 設計

### 5.1 API 路由總覽

所有 API 路由前綴：`/api/v1`

#### 🔐 認證 (Auth)

| Method | Path                    | 說明               |
| ------ | ----------------------- | ------------------ |
| POST   | `/auth/register`        | 註冊               |
| POST   | `/auth/login`           | 登入（回傳 JWT）   |
| POST   | `/auth/refresh`         | 刷新 Token         |
| POST   | `/auth/logout`          | 登出               |
| POST   | `/auth/forgot-password` | 寄送重設密碼信     |
| POST   | `/auth/reset-password`  | 重設密碼           |
| GET    | `/auth/me`              | 取得當前使用者資訊 |

#### 👤 使用者 (Users)

| Method | Path                   | 說明         |
| ------ | ---------------------- | ------------ |
| PUT    | `/users/profile`       | 更新個人資料 |
| PUT    | `/users/password`      | 修改密碼     |
| GET    | `/users/addresses`     | 取得地址清單 |
| POST   | `/users/addresses`     | 新增地址     |
| PUT    | `/users/addresses/:id` | 更新地址     |
| DELETE | `/users/addresses/:id` | 刪除地址     |
| GET    | `/users/cards`         | 取得儲存卡片 |
| POST   | `/users/cards`         | 新增卡片     |
| DELETE | `/users/cards/:id`     | 刪除卡片     |

#### 📦 商品 (Products)

| Method | Path                    | 說明                         |
| ------ | ----------------------- | ---------------------------- |
| GET    | `/products`             | 商品列表（分頁、篩選、排序） |
| GET    | `/products/:id`         | 商品詳情                     |
| GET    | `/products/search`      | 搜尋商品                     |
| GET    | `/products/trending`    | 熱門商品                     |
| GET    | `/products/:id/reviews` | 商品評價                     |
| POST   | `/products/:id/reviews` | 新增評價                     |

#### 🏷 分類 (Categories)

| Method | Path                         | 說明         |
| ------ | ---------------------------- | ------------ |
| GET    | `/categories`                | 分類樹狀結構 |
| GET    | `/categories/:slug/products` | 分類下商品   |

#### 🏢 品牌 (Brands)

| Method | Path                     | 說明            |
| ------ | ------------------------ | --------------- |
| GET    | `/brands`                | 品牌列表（A-Z） |
| GET    | `/brands/:slug`          | 品牌詳情        |
| GET    | `/brands/:slug/products` | 品牌下商品      |

#### 🛒 購物車 (Cart)

| Method | Path              | 說明       |
| ------ | ----------------- | ---------- |
| GET    | `/cart`           | 取得購物車 |
| POST   | `/cart/items`     | 加入商品   |
| PUT    | `/cart/items/:id` | 更新數量   |
| DELETE | `/cart/items/:id` | 移除商品   |
| POST   | `/cart/coupon`    | 套用優惠碼 |
| DELETE | `/cart/coupon`    | 移除優惠碼 |

#### 💳 訂單 (Orders)

| Method | Path                   | 說明         |
| ------ | ---------------------- | ------------ |
| POST   | `/orders`              | 建立訂單     |
| GET    | `/orders`              | 訂單列表     |
| GET    | `/orders/:id`          | 訂單詳情     |
| POST   | `/orders/:id/cancel`   | 取消訂單     |
| POST   | `/orders/:id/return`   | 申請退貨     |
| GET    | `/orders/:id/tracking` | 訂單物流追蹤 |

#### 💰 支付 (Payments)

| Method | Path                   | 說明                  |
| ------ | ---------------------- | --------------------- |
| POST   | `/payments/create`     | 建立支付              |
| POST   | `/payments/callback`   | 支付回調（綠界/藍新） |
| GET    | `/payments/:id/status` | 查詢支付狀態          |

#### ❤️ 願望清單 (Wishlist)

| Method | Path                   | 說明         |
| ------ | ---------------------- | ------------ |
| GET    | `/wishlist`            | 取得願望清單 |
| POST   | `/wishlist/:productId` | 加入願望清單 |
| DELETE | `/wishlist/:productId` | 移除         |

#### 🎯 回饋金 (Points)

| Method | Path             | 說明           |
| ------ | ---------------- | -------------- |
| GET    | `/points`        | 點數餘額與明細 |
| POST   | `/points/redeem` | 兌換折抵       |

#### 📢 首頁內容 (Content)

| Method | Path                         | 說明         |
| ------ | ---------------------------- | ------------ |
| GET    | `/content/banners`           | 首頁 Banner  |
| GET    | `/content/announcements`     | 公告列表     |
| GET    | `/content/featured-sections` | 首頁特色區塊 |

### 5.2 API 回應格式

```json
{
  "success": true,
  "data": { ... },
  "message": "操作成功",
  "meta": {
    "page": 1,
    "per_page": 20,
    "total": 150,
    "total_pages": 8
  }
}
```

錯誤回應：

```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "欄位驗證錯誤",
    "details": [{ "field": "email", "message": "Email 格式不正確" }]
  }
}
```

---

## 6. 資料庫設計

### 6.1 ER 模型核心資料表

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│    users     │     │   products   │     │   brands     │
├──────────────┤     ├──────────────┤     ├──────────────┤
│ id (PK)      │     │ id (PK)      │     │ id (PK)      │
│ email        │     │ brand_id(FK) │────►│ name         │
│ password_hash│     │ category_id  │     │ slug         │
│ first_name   │     │ name         │     │ logo_url     │
│ last_name    │     │ slug         │     │ description  │
│ phone        │     │ description  │     │ is_active    │
│ avatar_url   │     │ price        │     └──────────────┘
│ role         │     │ sale_price   │
│ is_active    │     │ sku          │     ┌──────────────┐
│ is_verified  │     │ stock        │     │  categories  │
│ points       │     │ images       │     ├──────────────┤
│ credits      │     │ is_active    │     │ id (PK)      │
│ created_at   │     │ is_featured  │     │ parent_id    │
│ updated_at   │     │ created_at   │     │ name         │
└──────┬───────┘     └──────────────┘     │ slug         │
       │                                   │ image_url    │
       │ 1:N                               │ sort_order   │
       ▼                                   └──────────────┘
┌──────────────┐     ┌──────────────┐
│   orders     │     │ order_items  │
├──────────────┤     ├──────────────┤
│ id (PK)      │     │ id (PK)      │
│ user_id (FK) │     │ order_id(FK) │
│ order_number │     │ product_id   │
│ status       │     │ variant_id   │
│ subtotal     │     │ quantity     │
│ discount     │     │ unit_price   │
│ shipping_fee │     │ subtotal     │
│ total        │     └──────────────┘
│ payment_method│
│ payment_status│
│ shipping_addr │
│ tracking_no  │
│ notes        │
│ created_at   │
│ updated_at   │
└──────────────┘
```

### 6.2 完整資料表清單

| 資料表                 | 說明                                     |
| ---------------------- | ---------------------------------------- |
| `users`                | 使用者（含管理員）                       |
| `user_addresses`       | 使用者地址                               |
| `user_cards`           | 儲存的付款卡片（僅 Token，不存完整卡號） |
| `products`             | 商品                                     |
| `product_variants`     | 商品規格（重量、口味等）                 |
| `product_images`       | 商品圖片                                 |
| `product_reviews`      | 商品評價                                 |
| `categories`           | 商品分類（自關聯樹狀結構）               |
| `brands`               | 品牌                                     |
| `orders`               | 訂單                                     |
| `order_items`          | 訂單明細                                 |
| `order_status_logs`    | 訂單狀態變更記錄                         |
| `cart_items`           | 購物車項目                               |
| `wishlists`            | 願望清單                                 |
| `coupons`              | 優惠碼                                   |
| `coupon_usages`        | 優惠碼使用記錄                           |
| `points_transactions`  | 回饋金交易紀錄                           |
| `credits_transactions` | 購物金交易紀錄                           |
| `banners`              | 首頁 Banner                              |
| `announcements`        | 公告                                     |
| `featured_sections`    | 首頁特色區塊                             |
| `payments`             | 支付紀錄                                 |
| `shipping_methods`     | 配送方式                                 |
| `return_requests`      | 退貨申請                                 |
| `audit_logs`           | 操作稽核日誌                             |
| `sessions`             | 使用者登入 Session（Redis 備份）         |

### 6.3 索引規劃

```sql
-- 商品搜尋效能
CREATE INDEX idx_products_name_gin ON products USING gin(to_tsvector('chinese', name));
CREATE INDEX idx_products_brand_id ON products(brand_id);
CREATE INDEX idx_products_category_id ON products(category_id);
CREATE INDEX idx_products_price ON products(price);
CREATE INDEX idx_products_created_at ON products(created_at DESC);
CREATE INDEX idx_products_is_active ON products(is_active) WHERE is_active = true;

-- 訂單查詢效能
CREATE INDEX idx_orders_user_id ON orders(user_id);
CREATE INDEX idx_orders_status ON orders(status);
CREATE INDEX idx_orders_created_at ON orders(created_at DESC);
CREATE INDEX idx_order_items_order_id ON order_items(order_id);

-- 使用者查詢
CREATE UNIQUE INDEX idx_users_email ON users(email);
```

---

## 7. 管理者後台

### 7.1 後台路由 (`/admin/*`)

管理員後台為前端 Vue 應用的獨立模組，使用相同技術棧，透過 Role-Based Access Control (RBAC) 控制權限。

### 7.2 後台功能模組

#### 📊 儀表板 (`/admin/dashboard`)

- 今日訂單數 / 營收
- 本週 / 本月銷售趨勢圖（Chart.js）
- 訂單狀態分佈圓餅圖
- 熱銷商品 Top 10
- 新會員統計
- 庫存不足警告
- 即時線上人數

#### 📦 商品管理 (`/admin/products`)

- 商品 CRUD（新增/編輯/刪除/上下架）
- 批次匯入/匯出（CSV/Excel）
- 圖片上傳與管理（拖曳排序）
- 規格管理（重量、口味）
- 庫存管理與警告閾值設定
- SEO 欄位（Meta Title、Description）

#### 🏷 分類管理 (`/admin/categories`)

- 樹狀結構拖曳調整
- 分類圖片上傳
- 分類啟用/停用

#### 🏢 品牌管理 (`/admin/brands`)

- 品牌 CRUD
- Logo 上傳
- 品牌描述 Rich Text Editor

#### 📋 訂單管理 (`/admin/orders`)

- 訂單列表（篩選：狀態、日期、金額）
- 訂單詳情檢視
- 訂單狀態更新（待付款 → 已付款 → 出貨中 → 已送達）
- 物流單號填寫
- 退貨處理
- 訂單匯出

#### 👥 會員管理 (`/admin/users`)

- 會員列表（搜尋、篩選）
- 會員詳情（訂單紀錄、點數、地址）
- 會員停權/啟用
- 手動調整回饋金/購物金
- 角色權限管理

#### 🎫 促銷管理 (`/admin/promotions`)

- 優惠碼 CRUD
- 設定折扣類型（百分比/固定金額）
- 設定使用條件（最低消費、限定商品、限定用戶）
- 有效期間
- 使用記錄追蹤

#### 📢 內容管理 (`/admin/content`)

- 首頁 Banner 管理（圖片、連結、排序、上下架時間）
- 公告管理
- 特色區塊管理

#### 📊 數據報表 (`/admin/reports`)

- 銷售報表（日/週/月）
- 商品銷售排行
- 品牌銷售分析
- 會員消費分析
- 退貨率分析
- 報表匯出（PDF/Excel）

#### ⚙️ 系統設定 (`/admin/settings`)

- 一般設定（網站名稱、Logo、聯絡資訊）
- 運費設定（免運門檻、各區費率）
- 付款方式管理
- Email 範本設定
- SEO 全站設定

### 7.3 管理員角色與權限

| 角色          | 說明       | 權限範圍                   |
| ------------- | ---------- | -------------------------- |
| `super_admin` | 超級管理員 | 全部功能                   |
| `admin`       | 管理員     | 商品、訂單、會員、內容管理 |
| `editor`      | 編輯者     | 商品、內容管理             |
| `viewer`      | 檢視者     | 僅檢視報表與列表           |

```python
# 權限裝飾器範例
@router.put("/products/{product_id}")
@require_permission("products:write")
async def update_product(product_id: int, data: ProductUpdate, admin: Admin = Depends(get_current_admin)):
    ...
```

---

## 8. 網路安全與個資保護

### 8.1 OWASP Top 10 防護

| 風險                     | 防護措施                                                  |
| ------------------------ | --------------------------------------------------------- |
| **注入攻擊 (Injection)** | SQLAlchemy ORM 參數化查詢、輸入驗證（Pydantic）           |
| **認證失效**             | JWT Token + Refresh Token、密碼 bcrypt 雜湊、登入嘗試限制 |
| **敏感資料暴露**         | HTTPS Only、密碼不回傳、卡號僅存 Token                    |
| **XXE**                  | FastAPI 預設不解析 XML                                    |
| **存取控制**             | RBAC 權限模型、API 層級權限檢查                           |
| **安全配置錯誤**         | 環境變數管理、生產移除 Debug Mode                         |
| **XSS**                  | Vue 自動轉義、CSP 標頭、DOMPurify                         |
| **反序列化不安全**       | Pydantic 嚴格型別驗證                                     |
| **使用含已知漏洞的元件** | Dependabot 自動掃描、定期更新                             |
| **日誌與監控不足**       | 結構化日誌、操作稽核記錄                                  |

### 8.2 個資保護（GDPR / 台灣個資法合規）

| 項目             | 實施方式                                 |
| ---------------- | ---------------------------------------- |
| **最小資料蒐集** | 僅蒐集購物必要資訊                       |
| **加密儲存**     | 敏感欄位 AES-256 加密，密碼 bcrypt       |
| **傳輸加密**     | 全站 HTTPS (TLS 1.3)                     |
| **存取控制**     | 使用者僅能存取自己的資料                 |
| **資料刪除**     | 提供帳號刪除功能，關聯資料匿名化         |
| **隱私政策**     | 完整隱私政策頁面，首次使用同意           |
| **Cookie 同意**  | Cookie Banner 告知使用者                 |
| **稽核日誌**     | 所有涉及個資操作記錄 audit_logs          |
| **資料匯出**     | 使用者可下載自己的所有資料（GDPR Right） |
| **密碼規則**     | 最少 8 碼，含大小寫+數字+特殊符號        |

### 8.3 安全標頭設定

```python
# FastAPI Middleware
app.add_middleware(
    CORSMiddleware,
    allow_origins=["https://popularfoodshop.vercel.app"],
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["Authorization", "Content-Type"],
    allow_credentials=True,
)

# Security Headers (via Nginx 或 Middleware)
headers = {
    "Strict-Transport-Security": "max-age=31536000; includeSubDomains",
    "X-Content-Type-Options": "nosniff",
    "X-Frame-Options": "DENY",
    "X-XSS-Protection": "1; mode=block",
    "Content-Security-Policy": "default-src 'self'; img-src 'self' https://cdn.popularfoodshop.com; script-src 'self'",
    "Referrer-Policy": "strict-origin-when-cross-origin",
    "Permissions-Policy": "camera=(), microphone=(), geolocation=()",
}
```

### 8.4 Rate Limiting

```python
# Redis-based Rate Limiting
RATE_LIMITS = {
    "login": "5/minute",          # 登入：每分鐘 5 次
    "register": "3/hour",         # 註冊：每小時 3 次
    "api_general": "100/minute",  # 一般 API：每分鐘 100 次
    "search": "30/minute",        # 搜尋：每分鐘 30 次
    "checkout": "10/minute",      # 結帳：每分鐘 10 次
}
```

### 8.5 支付安全

- PCI DSS 合規：不儲存完整信用卡號，使用第三方支付 Token
- 金流回調驗簽（HMAC-SHA256）
- 訂單金額後端驗證，防止前端竄改
- 支付冪等性（Idempotency Key）

---

## 9. 效能規劃（5000 人同時上線）

### 9.1 並發估算

| 指標                 | 數值         |
| -------------------- | ------------ |
| 同時上線人數         | 5,000        |
| 預估每秒請求數 (RPS) | ~500-1,000   |
| 平均 API 回應時間    | < 200ms      |
| 首頁載入時間（LCP）  | < 2.5s       |
| 資料庫 QPS           | ~2,000-5,000 |

### 9.2 前端效能優化

| 策略               | 實施方式                              |
| ------------------ | ------------------------------------- |
| **路由懶載入**     | `() => import('./views/Product.vue')` |
| **元件懶載入**     | `defineAsyncComponent`                |
| **圖片優化**       | WebP 格式、srcset 響應式、懶載入      |
| **CDN 加速**       | Vercel Edge Network / Cloudflare      |
| **Tree Shaking**   | Vite 自動移除未使用程式碼             |
| **Code Splitting** | 按路由自動分割                        |
| **快取策略**       | Service Worker、HTTP Cache Headers    |
| **壓縮**           | Brotli / Gzip (Vercel 自動)           |
| **預載入**         | `<link rel="preload">` 關鍵資源       |
| **骨架屏**         | 載入時顯示 Skeleton UI                |

### 9.3 後端效能優化

| 策略             | 實施方式                                      |
| ---------------- | --------------------------------------------- |
| **非同步處理**   | FastAPI async/await                           |
| **多 Worker**    | Uvicorn 4 Workers（依 CPU 核心數）            |
| **Redis 快取**   | 商品列表、分類樹、品牌快取（TTL 5min）        |
| **資料庫連線池** | SQLAlchemy AsyncSession Pool (min=10, max=50) |
| **查詢優化**     | 避免 N+1、使用 `selectinload`/`joinedload`    |
| **分頁限制**     | 限制每頁最多 50 筆                            |
| **全文搜尋**     | PostgreSQL GIN Index + `tsvector`             |
| **背景任務**     | Celery 處理寄信、報表、圖片壓縮               |
| **回應壓縮**     | GzipMiddleware                                |
| **API 結果快取** | 熱門商品/首頁內容 Redis 快取                  |

### 9.4 資料庫效能優化

| 策略            | 實施方式                                        |
| --------------- | ----------------------------------------------- |
| **連線池**      | SQLAlchemy Pool (pool_size=20, max_overflow=30) |
| **讀寫分離**    | 主庫寫入、副本讀取（未來擴展）                  |
| **索引優化**    | 參見 6.3                                        |
| **查詢分析**    | `EXPLAIN ANALYZE` 定期檢視慢查詢                |
| **資料分區**    | 訂單表依月份分區（訂單量大時）                  |
| **定期 VACUUM** | PostgreSQL 自動清理                             |

### 9.5 快取策略

```
┌─────────┐     ┌─────────┐     ┌──────────┐     ┌──────────┐
│ Browser │────►│ Vercel  │────►│  Redis   │────►│PostgreSQL│
│ Cache   │     │ CDN     │     │ Cache    │     │          │
│ (SW)    │     │ (Edge)  │     │ (L2)     │     │ (Source) │
└─────────┘     └─────────┘     └──────────┘     └──────────┘
    L1              L1.5             L2               L3
  (Client)       (CDN Edge)     (App Cache)      (Database)
```

| 資料類型     | 快取時間 | 快取位置        |
| ------------ | -------- | --------------- |
| 首頁 Banner  | 10 min   | Redis + CDN     |
| 分類樹       | 30 min   | Redis           |
| 品牌列表     | 30 min   | Redis           |
| 商品列表     | 5 min    | Redis           |
| 商品詳情     | 5 min    | Redis           |
| 使用者購物車 | 即時     | Redis           |
| 搜尋結果     | 3 min    | Redis           |
| 靜態資源     | 1 year   | CDN (immutable) |

---

## 10. 部署策略（Vercel）

### 10.1 前端部署（Vercel）

```yaml
# vercel.json
{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers":
    [
      {
        "source": "/assets/(.*)",
        "headers":
          [
            {
              "key": "Cache-Control",
              "value": "public, max-age=31536000, immutable",
            },
          ],
      },
      {
        "source": "/(.*)",
        "headers":
          [
            { "key": "X-Frame-Options", "value": "DENY" },
            { "key": "X-Content-Type-Options", "value": "nosniff" },
            {
              "key": "Strict-Transport-Security",
              "value": "max-age=31536000; includeSubDomains",
            },
          ],
      },
    ],
}
```

### 10.2 後端部署選項

| 平台                | 特點                                     | 適用情境   |
| ------------------- | ---------------------------------------- | ---------- |
| **Railway**         | Docker 部署、PostgreSQL 內建、自動 HTTPS | 推薦首選   |
| **Render**          | 免費層、自動部署、PostgreSQL             | 小團隊     |
| **Fly.io**          | 全球邊緣部署、Docker                     | 低延遲需求 |
| **AWS ECS/Fargate** | 企業級、高彈性                           | 大規模擴展 |

### 10.3 CI/CD 流程

```yaml
# .github/workflows/deploy.yml
name: Deploy

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Backend Tests
        run: |
          cd backend
          pip install -r requirements.txt
          pytest --cov
      - name: Run Frontend Tests
        run: |
          cd frontend
          npm ci
          npm run test
          npm run lint

  deploy-frontend:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}

  deploy-backend:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Railway
        run: railway up
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
```

### 10.4 環境變數管理

```bash
# Frontend (.env.production)
VITE_API_BASE_URL=https://api.popularfoodshop.com
VITE_APP_TITLE=Popular Food Shop
VITE_GA_ID=G-XXXXXXXXXX

# Backend (.env)
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/dbname
REDIS_URL=redis://host:6379/0
SECRET_KEY=<random-256-bit-key>
JWT_SECRET=<random-256-bit-key>
JWT_EXPIRY=3600
REFRESH_TOKEN_EXPIRY=604800
CORS_ORIGINS=https://popularfoodshop.vercel.app
S3_BUCKET=popularfoodshop-assets
S3_ACCESS_KEY=xxx
S3_SECRET_KEY=xxx
PAYMENT_MERCHANT_ID=xxx
PAYMENT_HASH_KEY=xxx
PAYMENT_HASH_IV=xxx
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=xxx
SMTP_PASS=xxx
```

---

## 11. Chrome MCP 驗證計畫

開發完成後，使用 Chrome MCP（Model Context Protocol）進行自動化驗證，確保所有頁面與功能正常運作。

### 11.1 驗證項目清單

#### 🏠 首頁驗證

- [ ] 頁面正常載入，無 Console 錯誤
- [ ] Hero Banner 輪播正常運作
- [ ] 所有圖片正確載入（無 broken images）
- [ ] 導覽列功能正常（搜尋、會員、購物車）
- [ ] Mega Menu 展開/收合正常
- [ ] 分類卡片點擊跳轉正確
- [ ] 熱搜標籤可滾動、可點擊
- [ ] 商品輪播滑動正常
- [ ] Footer 連結全部可用
- [ ] 響應式設計：手機/平板/桌機尺寸

#### 📦 商品功能驗證

- [ ] 商品列表正確載入、分頁正常
- [ ] 篩選功能（品牌、價格、分類）即時生效
- [ ] 排序功能正確
- [ ] 商品卡片顯示：圖片、品牌、名稱、價格、折扣標籤
- [ ] 商品詳情頁資訊完整
- [ ] 圖片輪播/縮放正常
- [ ] 加入購物車成功，購物車數量更新
- [ ] 加入願望清單成功

#### 🛒 購物流程驗證

- [ ] 購物車頁面：商品列表、數量調整、刪除
- [ ] 優惠碼套用與計算正確
- [ ] 結帳流程：收件資訊 → 配送 → 付款 → 確認
- [ ] 表單驗證運作正常
- [ ] 訂單建立成功，顯示訂單編號

#### 👤 會員功能驗證

- [ ] 註冊流程正常（含欄位驗證）
- [ ] 登入/登出正常
- [ ] 個人資料修改
- [ ] 地址簿 CRUD
- [ ] 訂單紀錄查看
- [ ] 回饋金餘額顯示

#### 🔐 安全性驗證

- [ ] HTTPS 連線正常
- [ ] 未登入時無法存取會員頁面（重導登入）
- [ ] JWT Token 過期後自動刷新或重導登入
- [ ] API 回應不包含敏感欄位（密碼）
- [ ] CORS 設定正確（僅允許前端域名）
- [ ] CSP Header 存在
- [ ] 無 XSS 漏洞（嘗試注入 script）

#### ⚡ 效能驗證

- [ ] 首頁 LCP < 2.5s
- [ ] FID < 100ms
- [ ] CLS < 0.1
- [ ] API 回應時間 < 200ms
- [ ] 圖片載入使用 lazy loading
- [ ] 無不必要的網路請求

#### 🖥 管理後台驗證

- [ ] 管理員登入正常
- [ ] 儀表板數據正確載入
- [ ] 商品 CRUD 全流程
- [ ] 訂單狀態更新
- [ ] 會員管理操作
- [ ] 報表匯出

### 11.2 Chrome MCP 測試腳本範例

```javascript
// 使用 Chrome MCP 進行自動化驗證
// 1. 首頁載入測試
await navigate("https://popularfoodshop.vercel.app");
await takeScreenshot("homepage"); // 截圖記錄
await checkConsoleErrors(); // 檢查 Console 錯誤

// 2. 註冊流程測試
await navigate("/register");
await fill("#email", "test@example.com");
await fill("#password", "Test1234!");
await fill("#confirmPassword", "Test1234!");
await click("button[type='submit']");
await waitFor("text=註冊成功");

// 3. 商品瀏覽測試
await navigate("/category/snacks");
await waitFor(".product-card");
await click(".product-card:first-child");
await waitFor(".product-detail");
await takeScreenshot("product-detail");

// 4. 購物車測試
await click("#add-to-cart");
await waitFor("text=已加入購物車");
await navigate("/cart");
await waitFor(".cart-item");
await takeScreenshot("cart");

// 5. 響應式測試
await resize(375, 812); // iPhone
await takeScreenshot("homepage-mobile");
await resize(768, 1024); // iPad
await takeScreenshot("homepage-tablet");
await resize(1920, 1080); // Desktop
await takeScreenshot("homepage-desktop");
```

---

## 12. 開發時程與里程碑

### Phase 1：基礎建設（第 1-2 週）

| 任務        | 說明                                    |
| ----------- | --------------------------------------- |
| 專案初始化  | Vite + Vue 3 + Tailwind + Pinia 腳手架  |
| 後端初始化  | FastAPI 專案結構 + SQLAlchemy + Alembic |
| 資料庫建立  | PostgreSQL 設定、資料表建立、種子資料   |
| Docker 環境 | docker-compose（DB + Redis + Backend）  |
| 設計系統    | Tailwind 主題設定、基礎 UI 元件庫       |

### Phase 2：核心功能（第 3-5 週）

| 任務     | 說明                   |
| -------- | ---------------------- |
| 認證系統 | 註冊/登入/JWT/忘記密碼 |
| 首頁開發 | 所有首頁區塊元件       |
| 商品模組 | 列表/詳情/搜尋/篩選    |
| 購物車   | 加入/修改/刪除/優惠碼  |
| 後端 API | 完成所有核心 API       |

### Phase 3：交易與會員（第 6-7 週）

| 任務       | 說明                            |
| ---------- | ------------------------------- |
| 結帳流程   | 多步驟結帳表單                  |
| 金流串接   | 綠界/藍新/LINE Pay              |
| 訂單管理   | 訂單建立/查詢/狀態更新          |
| 會員中心   | 個人資料/地址/訂單紀錄/願望清單 |
| 回饋金系統 | 點數累積/兌換/到期              |

### Phase 4：管理後台（第 8-9 週）

| 任務          | 說明               |
| ------------- | ------------------ |
| 管理後台 UI   | 儀表板/側邊欄佈局  |
| 商品管理      | CRUD + 批次操作    |
| 訂單管理      | 列表/狀態/物流     |
| 會員管理      | 列表/詳情/停權     |
| 促銷/內容管理 | 優惠碼/Banner/公告 |
| 報表系統      | 銷售/商品/會員分析 |

### Phase 5：最佳化與安全（第 10 週）

| 任務         | 說明                                |
| ------------ | ----------------------------------- |
| 效能優化     | 快取、懶載入、Code Splitting        |
| 安全加固     | Rate Limiting、CSP、CORS            |
| SEO 優化     | Meta Tags、Structured Data、Sitemap |
| 多語系       | 中文/英文切換                       |
| 無障礙(a11y) | ARIA Labels、鍵盤導覽               |

### Phase 6：測試與部署（第 11-12 週）

| 任務            | 說明                           |
| --------------- | ------------------------------ |
| 單元測試        | Vitest（前端）+ pytest（後端） |
| E2E 測試        | Playwright 全流程測試          |
| Chrome MCP 驗證 | 全站自動化驗收                 |
| 壓力測試        | Locust 5000 並發模擬           |
| 部署上線        | Vercel + Railway               |
| 監控設定        | Sentry 錯誤追蹤 + UptimeRobot  |

---

## 13. 目錄結構

```
popularfoodshop/
├── frontend/                          # 前端 Vue 專案
│   ├── public/
│   │   ├── favicon.ico
│   │   └── robots.txt
│   ├── src/
│   │   ├── assets/                    # 靜態資源
│   │   │   ├── images/
│   │   │   └── styles/
│   │   │       └── main.css           # Tailwind 入口
│   │   ├── components/                # Vue 元件（見 4.2）
│   │   │   ├── layout/
│   │   │   ├── home/
│   │   │   ├── product/
│   │   │   ├── cart/
│   │   │   ├── checkout/
│   │   │   ├── account/
│   │   │   ├── admin/
│   │   │   └── ui/
│   │   ├── composables/               # Composition API 共用邏輯
│   │   │   ├── useAuth.js
│   │   │   ├── useCart.js
│   │   │   ├── useProduct.js
│   │   │   └── useToast.js
│   │   ├── layouts/                   # 頁面佈局
│   │   │   ├── DefaultLayout.vue
│   │   │   ├── AdminLayout.vue
│   │   │   └── AuthLayout.vue
│   │   ├── pages/                     # 頁面元件 (路由對應)
│   │   │   ├── Home.vue
│   │   │   ├── ProductList.vue
│   │   │   ├── ProductDetail.vue
│   │   │   ├── Cart.vue
│   │   │   ├── Checkout.vue
│   │   │   ├── Login.vue
│   │   │   ├── Register.vue
│   │   │   ├── account/
│   │   │   └── admin/
│   │   ├── router/                    # Vue Router 設定
│   │   │   └── index.js
│   │   ├── stores/                    # Pinia Stores
│   │   │   ├── auth.js
│   │   │   ├── cart.js
│   │   │   ├── product.js
│   │   │   ├── wishlist.js
│   │   │   ├── checkout.js
│   │   │   └── ui.js
│   │   ├── services/                  # API 服務層
│   │   │   ├── api.js                 # Axios 實例
│   │   │   ├── authService.js
│   │   │   ├── productService.js
│   │   │   ├── cartService.js
│   │   │   ├── orderService.js
│   │   │   └── adminService.js
│   │   ├── utils/                     # 工具函式
│   │   │   ├── formatPrice.js
│   │   │   ├── validators.js
│   │   │   └── constants.js
│   │   ├── i18n/                      # 多語系
│   │   │   ├── index.js
│   │   │   ├── zh-TW.json
│   │   │   └── en.json
│   │   ├── App.vue
│   │   └── main.js
│   ├── tests/                         # 前端測試
│   │   ├── unit/
│   │   └── e2e/
│   ├── index.html
│   ├── vite.config.js
│   ├── tailwind.config.js
│   ├── postcss.config.js
│   ├── package.json
│   ├── vercel.json
│   └── .env.example
│
├── backend/                           # 後端 FastAPI 專案
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                    # FastAPI 入口
│   │   ├── config.py                  # 設定管理
│   │   ├── database.py                # 資料庫連線
│   │   ├── dependencies.py            # 共用依賴
│   │   ├── models/                    # SQLAlchemy Models
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── product.py
│   │   │   ├── category.py
│   │   │   ├── brand.py
│   │   │   ├── order.py
│   │   │   ├── cart.py
│   │   │   ├── payment.py
│   │   │   ├── coupon.py
│   │   │   ├── content.py
│   │   │   └── audit.py
│   │   ├── schemas/                   # Pydantic Schemas
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   ├── product.py
│   │   │   ├── order.py
│   │   │   ├── cart.py
│   │   │   └── common.py
│   │   ├── routers/                   # API 路由
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── users.py
│   │   │   ├── products.py
│   │   │   ├── categories.py
│   │   │   ├── brands.py
│   │   │   ├── cart.py
│   │   │   ├── orders.py
│   │   │   ├── payments.py
│   │   │   ├── wishlist.py
│   │   │   ├── points.py
│   │   │   ├── content.py
│   │   │   └── admin/
│   │   │       ├── __init__.py
│   │   │       ├── dashboard.py
│   │   │       ├── products.py
│   │   │       ├── orders.py
│   │   │       ├── users.py
│   │   │       ├── promotions.py
│   │   │       ├── content.py
│   │   │       └── reports.py
│   │   ├── services/                  # 業務邏輯層
│   │   │   ├── __init__.py
│   │   │   ├── auth_service.py
│   │   │   ├── product_service.py
│   │   │   ├── order_service.py
│   │   │   ├── payment_service.py
│   │   │   ├── email_service.py
│   │   │   ├── cache_service.py
│   │   │   └── file_service.py
│   │   ├── middleware/                # 中間件
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── rate_limit.py
│   │   │   ├── security_headers.py
│   │   │   └── logging.py
│   │   ├── utils/                     # 工具函式
│   │   │   ├── __init__.py
│   │   │   ├── security.py
│   │   │   ├── pagination.py
│   │   │   └── helpers.py
│   │   └── tasks/                     # Celery 任務
│   │       ├── __init__.py
│   │       ├── email_tasks.py
│   │       ├── report_tasks.py
│   │       └── image_tasks.py
│   ├── alembic/                       # 資料庫遷移
│   │   ├── versions/
│   │   └── env.py
│   ├── tests/                         # 後端測試
│   │   ├── conftest.py
│   │   ├── test_auth.py
│   │   ├── test_products.py
│   │   ├── test_orders.py
│   │   └── test_admin.py
│   ├── seeds/                         # 種子資料
│   │   ├── seed_categories.py
│   │   ├── seed_brands.py
│   │   └── seed_products.py
│   ├── alembic.ini
│   ├── requirements.txt
│   ├── Dockerfile
│   └── .env.example
│
├── docker-compose.yml                 # 開發環境容器編排
├── docker-compose.prod.yml            # 生產環境容器編排
├── .github/
│   └── workflows/
│       ├── test.yml                   # CI 測試
│       └── deploy.yml                 # CD 部署
├── docs/                              # 專案文件
│   ├── api.md                         # API 文件
│   ├── database.md                    # 資料庫文件
│   └── deployment.md                  # 部署文件
├── skills.md                          # 本文件
├── .gitignore
└── README.md
```

---

## 附錄：技術決策紀錄

| 決策     | 選擇                           | 原因                                         |
| -------- | ------------------------------ | -------------------------------------------- |
| 前端框架 | Vue 3 + Composition API        | 使用者指定                                   |
| 建置工具 | Vite                           | 使用者指定，開發速度快                       |
| CSS 框架 | Tailwind CSS                   | 使用者指定，原子化 CSS 高效開發              |
| 狀態管理 | Pinia                          | 使用者指定，輕量直覺                         |
| 後端框架 | FastAPI                        | Python 生態最快非同步框架，自帶 OpenAPI 文件 |
| ORM      | SQLAlchemy 2.x                 | Python 最成熟 ORM，支援 async                |
| 資料庫   | PostgreSQL                     | 使用者指定，支援全文搜尋、JSON               |
| 快取     | Redis                          | 標準快取方案，支援 Session/Rate Limit        |
| 認證     | JWT + Refresh Token            | 前後端分離標準方案                           |
| 部署     | Vercel (前端) + Railway (後端) | 使用者指定 Vercel，Railway 適合 Python 後端  |
| 檔案儲存 | Cloudflare R2                  | S3 相容，免出站流量費                        |
| 支付     | 綠界 ECPay                     | 台灣市場最普及金流                           |

---

> **文件版本：** v1.0  
> **最後更新：** 2026-02-21  
> **作者：** Popular Food Shop Development Team
